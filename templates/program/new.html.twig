{# templates/category/new.html.twig #}
{% extends "base.html.twig" %}

{% block body %}
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
    <input type='submit' value='Ajouter une nouvelle série'>
    {{ form_end(form) }}
    <a href=" {{ path('program_index') }}">Retour</a>
{% endblock %}